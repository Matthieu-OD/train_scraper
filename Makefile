# list of the command we want to add
#
# create these command per services and also globally
# linting
# formatting
# make up -> build docker containers for the api ( use the docker compose file )
# make start ( start the containers for the app )
#
# find the right level of simplification


# use docker-compose in dev and docker swarm in prod
# what about staging ? it will be swarm too
